# Runtime and transport dependencies
pyserial >= 3.4
pyserial-asyncio
aiocoap >= 0.4.17, < 1
PyYAML
pyxdg

# Optional passive LAN gateway discovery
zeroconf

# Test and distribution tooling
pytest
build
twine
